# Claudio 个人 AI 电台系统架构说明书

## 1. 核心愿景 (Core Vision)
Claudio 是一个深度集成的个人 AI 电台系统，其核心逻辑是：**“读懂听歌习惯 → 规划声音内容 → 像 DJ 那样播报”**。它不仅是一个音乐播放器，更是一个具备环境感知和个性化表达能力的智能音频伙伴。

## 2. 分层架构解读

### 第一层：外部上下文 (External Context - 输入层)
该层负责收集所有原始素材和环境信号，为 AI 提供决策依据。
* **用户侧 (USER):** 利用本地文件（如 `taste.md`, `routines.md`, `playlists.json`）构建用户画像，确保电台的“品味”完全私有化。
* **大脑侧 (BRAIN):** 采用 `Claude Code` 作为逻辑中枢，通过子进程调用和 JSON 输出实现复杂的推理任务。
* **音乐侧 (MUSIC):** 对接网易云音乐 API (NeteaseCloudMusicApi)，负责歌曲检索、链接获取及歌词匹配。
* **交互侧 (VOICE I/O):** 整合 Fish Audio (TTS)、飞书 (Lark)、天气信息及 UPnP 设备（如 Naim 客厅功放），实现感官的延伸。

### 第二层：本地大脑 (Local Brain - 逻辑层)
负责将第一层的原始数据转化为可执行指令的核心后端逻辑。
* **ROUTER.JS (意图分流):** 区分简单指令（如“播放周杰伦”）与自然语言交互（交给 Claude 处理）。
* **CONTEXT.JS (提示词组装):** 将用户语料、环境状态、历史记忆与系统 Prompt 动态合成。
* **CLAUDE.JS (大脑适配器):** 封装与 AI 的交互，解析包含 `say` (播报内容)、`play[]` (播放列表) 和 `segue` (转场逻辑) 的结构化数据。
* **SCHEDULER.JS (节律调度):** 驱动系统的主动性，例如在 07:00 进行日历规划，09:00 进行早间播报。
* **TTS.JS (声音管线):** 将 AI 生成的文本转化为音频流，并进行本地哈希缓存以优化性能。
* **STATE.DB (状态记忆):** 使用本地数据库持久化存储对话、计划和偏好，确保跨重启的一致性。

### 第三层：运行时聚合 (Runtime Aggregation - 执行层)
这是系统生成单次响应的“加工黑盒”。
* **Context Window 组装:** 每次触发均粘合六个片段：
    1. 系统提示词 (DJ Persona)
    2. 用户语料 (User Data)
    3. 环境注入 (Weather/Calendar)
    4. 检索记忆 (State DB)
    5. 用户输入/工具结果
    6. 执行轨迹 (Scheduler)
* **Model 前向过程:** 计算片段并输出执行包，随后通过 WebSocket 推送 `now-playing` 状态，并完成 TTS 合成与 NCM 队列解析。

### 第四层：交互表层 (Interaction - 表现层)
* **PWA (Progressive Web App):** 运行于 `localhost:8080` 的前端应用。提供播放器、个人资料和设置视图。具备 10 秒音频预加载能力，支持流式聊天。
* **HTTP Contract:** 前后端通过 6 条主要线路（API 路由）进行通讯，涵盖聊天、计划、品味及实时流媒体。

## 3. 技术方向与特点
* **私有化与本地化:** 倾向于使用 Markdown 文件和本地数据库而非云端数据库，保障用户隐私。
* **高度工程化:** 利用 `Claude Code` 的子进程调用绕过标准 API 限制，体现了极高的实用主义开发思路。
* **拟人化体验:** 强调“转场 (segue)”和“理由 (reason)”，使 AI 表现得更像一个有思想的真人 DJ。
